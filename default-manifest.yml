# The aerobatic.yml manifest file is where you specify metadata about
# your website including plugins that provided enhanced
# functionality at runtime on the Aerobatic platform.

id: __appId__

deploy:
  ignore: []
  directory: .

router:
  # The webpage plugin provides the core functionality for serving
  # .html files
  - module: webpage
